name: diario_app
description: "Una aplicación de diario personal con Flutter y Firebase"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  intl: ^0.18.1
  shared_preferences: ^2.2.1
  uuid: ^4.0.0
  image_picker: ^1.0.4
  path_provider: ^2.1.1
  fl_chart: ^0.63.0
  permission_handler: ^12.0.0+1
  intl_utils: ^2.8.10
  
  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/